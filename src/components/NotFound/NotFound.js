import { Typography } from '@material-ui/core';
import React from 'react';

const NotFound = () => {
  return (
    <Typography>
      Route Not Found
    </Typography>
  );
};

export default NotFound;